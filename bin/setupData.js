"use strict";

const path = require('path');

let project_name = path.basename(process.cwd());

module.exports = {
  version: '0.1.0',
  project_name: project_name,
  description: '',
  author: ''
};
